# The frontend requires NO environment variables.
# All data fetching (CISA catalog, NVD scores, news, breaches) goes through the
# backend API on the same origin. API keys and the catalog URL live in backend/.env.
